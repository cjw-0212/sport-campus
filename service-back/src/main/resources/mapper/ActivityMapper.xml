<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.serviceback.mapper.ActivityMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.serviceback.po.Activity">
        <id column="id" property="id"/>
        <result column="content" property="content"/>
        <result column="publish_user_id" property="publishUserId"/>
        <result column="photo" property="photo"/>
        <result column="total_num" property="totalNum"/>
        <result column="join_num" property="joinNum"/>
        <result column="position" property="position"/>
        <result column="is_card" property="isCard"/>
        <result column="is_finish" property="isFinish"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, content, publish_user_id, total_num, join_num, position, is_card, is_finish, create_time, update_time
    </sql>

</mapper>
